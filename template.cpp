#include <bits/stdc++.h>
using std::cin;
using std::cout;

int T;
void solve()
{
}

int main()
{
    freopen("input.txt", "r", stdin);
    std::ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> T;
    while (T--)
    {
        solve();
    }
    return 0;
}